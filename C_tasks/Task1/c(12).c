//C program to find square root of a number
#include <stdio.h>
int main()
{   int num ,root;

    printf("enter number :");
    scanf("%d",&num);

    root = sqrt(num);

    printf("Square root is %d",root);
    return 0;
}

